<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="BookOrder.Views.ManageBooksPage">

    <Grid Margin="20" RowDefinitions="Auto,Auto,*">
        <!-- Suchbereich -->
        <StackPanel Orientation="Horizontal" Spacing="10" Grid.Row="0">
            <TextBox x:Name="SearchBox" Width="200" Watermark="Titel oder Autor"/>
            <Button x:Name="BtnSearch" Content="Suchen" Width="100"/>
            <Button x:Name="BtnRefresh" Content="Aktualisieren" Width="120"/>
            <Button x:Name="BtnBack" Content="⬅ Zurück" Width="120"/>
        </StackPanel>

        <!-- Hinzufügen -->
        <StackPanel Orientation="Horizontal" Spacing="10" Grid.Row="1" Margin="0,10,0,10">
            <TextBox x:Name="TitleBox" Width="200" Watermark="Titel"/>
            <TextBox x:Name="AuthorBox" Width="200" Watermark="Autor"/>
            <TextBox x:Name="CodeBox" Width="160" Watermark="Code (8-stellig)"/>
            <Button x:Name="BtnAdd" Content="Hinzufügen" Width="150"/>
        </StackPanel>

        <!-- Buchliste -->
        <ListBox x:Name="BooksListBox" Grid.Row="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{Binding Title}" Width="200"/>
                        <TextBlock Text="{Binding Author}" Width="200"/>
                        <TextBlock Text="{Binding BookCode}" Width="150"/>
                        <Button Content="🗑 Löschen" Background="DarkRed" Width="150" Click="BtnDelete_Click"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
